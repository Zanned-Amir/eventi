import { join } from 'path';

export const File_UPLOAD_PATH = join(
  process.cwd(),
  'apps',
  'eventi',
  'src',
  'uploads',
);
export const USER_UPLOAD_PATH = 'users';
export const EVENT_UPLOAD_PATH = 'events';
export const ARTIST_UPLOAD_PATH = 'artists';
export const CONCERT_MEMBER_UPLOAD_PATH = 'concert-members';
